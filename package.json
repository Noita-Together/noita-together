{
  "name": "noita-together",
  "private": true,
  "version": "0.9.40",
  "packageManager": "yarn@3.5.1",
  "workspaces": [
    "nt-app/",
    "nt-web-app/",
    "nt-server/",
    "shared-lib/"
  ],
  "scripts": {
    "client": "yarn workspace nt-app electron:serve",
    "serverInitOnce": "node create-jwt-secret.js",
    "server": "yarn workspace nt-web-app serve",
    "serverDev": "yarn workspace nt-web-app serveDev",
    "buildClient": "yarn workspace nt-app electron:build",
    "regenerate-proto": "yarn workspace nt-web-app type-gen"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
